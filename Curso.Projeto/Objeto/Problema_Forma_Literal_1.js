const d1 = {
    dia: 9,
     mes: 10,
     ano: 2010,
     exibir: function (){
        return `${this.dia}/${this.mes}/${this.ano}`;
     }
};

const d2 = {
    dia: 22,
     mes: 11,
     ano: 2012,
     exibir: function (){
        return `${this.dia}/${this.mes}/${this.ano}`;
     }
};

const d3 = {
    dia: 25,
     mes: 12,
     ano: 2018,
     exibir: function (){
        return `${this.dia}/${this.mes}/${this.ano}`;
     }
};