// vírgulas finais
// É quando você coloca uma vírgula depois do último item de um objeto, array,
// unção ou parâmetro — mesmo sem precisar.

//Exemplo
const pessoa = {
  nome: "Renan",
  idade: 20,
};

// Arrays
const numeros = [1, 2, 3,];
console.log(numeros); // [1, 2, 3]

// Objetos
const user = {
  nome: "Renan",
  idade: 20,
  cidade: "São Paulo",
};

// Parâmetros de função
function mostrar(a, b, c,) {
  console.log(a, b, c);
}

mostrar(1, 2, 3,);

// Chamadas de função
console.log(
  "Renan",
  20,
);
